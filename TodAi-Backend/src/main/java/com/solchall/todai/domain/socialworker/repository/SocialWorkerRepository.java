package com.solchall.todai.domain.socialworker.repository;

import com.solchall.todai.domain.socialworker.entity.SocialWorker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SocialWorkerRepository extends JpaRepository<SocialWorker, Long> {
}
